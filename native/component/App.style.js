import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    app: {
        flex: 1,
    },
    appMain: {
        flex: 1,
        padding: 8,
    },
    appAction: {
        paddingVertical: 16,
    },
});

export default styles;
