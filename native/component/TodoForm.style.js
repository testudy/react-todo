import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    input: {
        margin: 0,
        padding: 8,
        color: '#666',
        fontSize: 16,
        borderWidth: 1,
        borderColor: '#ccc',
        borderRadius: 6,
    },
});

export default styles;
